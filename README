Run `$ ruby lib/tweet_generator.rb` to get a markov generated random tweet.
Default twitter handle is `"justinbieber"`, but can easily augment to take
an argument via terminal.

`secrets.json` file is omitted. Please add your own `secrets.json` file that
defines `CONSUMER_KEY`, `CONSUMER_SECRET`, `ACCESS_TOKEN`, &
`ACCESS_TOKEN_SECRET`.
